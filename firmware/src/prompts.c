// Common disp prompts

int bar(lcd * disp, char * prompt, int * value) {
	// Clear the display
	disp->clear();

	// Print the menu prompt
	
	// Read current value and determine how many bars to display
	
	// Print menu bars

	// Loop until select button has been pushed
	while(!select_button) {
		// If left or right button increment or decrement value
		if (right_button | left_button) {
		
		if (right_button) {

		}
		
		if (left_button) {
		
		}

		// Update disp to reflect new value

	}
}
